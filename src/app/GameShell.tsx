import React from 'react'

export default function GameShell(): JSX.Element {
    return (
        <div style={{ padding: 16 }}>
            <p>Game shell placeholder — engine and UI components go under <code>src/</code>.</p>
        </div>
    )
}
